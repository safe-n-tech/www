{
  "text": "{{ .Params.text }}",
  "userAnswer": null,
  "isCorrect": null,
  "correction": "{{ .Params.correction }}",
  "image": "{{ .Params.image }}",
  "choices": {{ .Params.choices | jsonify }},
  {{ $goodPractices := (where .Site.Pages "Section" "good-practices") }}
  "goodPractices": [
    {{- range $index, $goodPracticesLinked := (where $goodPractices ".Title" "in" .Params.goodPractices) -}}
    {{- if $index -}},{{- end -}} {{ .Render "single" }}
    {{- end -}}
  ]
}